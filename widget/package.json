{
  "name": "@messaging-ai/widget",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone embeddable chat widget for messaging-ai. Single-script embed, Shadow-DOM-isolated, Preact + Vite library build.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/check-bundle-size.mjs",
    "preview": "vite preview",
    "check:size": "node scripts/check-bundle-size.mjs"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "rollup-plugin-visualizer": "^5.12.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.10"
  }
}
